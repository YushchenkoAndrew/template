apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-ingress
  namespace: template
spec:
  rules:
  - host: mortis-grimreaper.ddns.net
    http:
      paths:
      - backend:
          serviceName: web-service
          servicePort: 8000
        path: /projects
